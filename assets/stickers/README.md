Drop your own sticker images here (scans, your own photos, or explicitly
CC0/licensed stickers only — see CONTEXT.md, "Scrapbook sticker decorations"
decision, for why: never Pinterest/stock cutouts you don't hold rights to).

Not wired into the site yet — the current per-step decorations are CSS-only
(`.paper-tag` in css/style.css). Once real files land here, reference them
from index.html / css as background-image or <img>, same pattern as
assets/photos/.
